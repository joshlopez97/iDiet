<!DOCTYPE html>
<html>

<head>
  <title>Sign Up</title>
  <% include ../partials/head %>
  <link rel="stylesheet" type="text/css" 
    href="/css/login.css">
  <link rel="stylesheet" type="text/css"
        href="/css/signup.css">
  <script>
    $(document).ready(function() {
      $(".back-btn").click(function () {
        window.location.href = "/start"
      });
      $("#height").on("input", function() {
        const value = $(this).val(),
              lastValue = $(this).data("lastVal");

        // Allow user to backspace without autoformatting
        if (lastValue && lastValue.length > value.length)
        {
          $(this).data("lastVal", $(this).val());
          return;
        }

        const heightRegex = /^([1-9]) ?'? ?([0-9]| ?1[01])$/g;

        // Change "5" to "5 '" automatically
        if (value.match(/^[1-9] *$/))
        {
          $(this).val(value.charAt(0) + ' \' ');
        }
        // Changes "62", "6 2", "6'2", ... all to "6 ' 2"
        else if (value.match(heightRegex))
        {
          const matches = heightRegex.exec(value),
                feet    = matches[1],
                inches  = matches[2];
          $(this).val(feet + ' \' ' + inches);
        }
        // Revert to last recorded value if input is invalid
        else
        {
          $(this).val(lastValue? lastValue : "");
        }
        $(this).data("lastVal", $(this).val());
      });
    });
  </script>
</head>
<body style="background: #fff">
  <div class="login-wrapper">
      <div class="start-page-holder">
        <div class="back-btn"></div>
        <img id="logo" class="logo-top-center" src="/img/icon.png">
      </div>
      <div class="form-holder">

        <form enctype="application/x-www-form-urlencoded" method="post" class="signup-form">
            <div class="login-header">
              <h3>Sign Up</h3>
              <h5>To get started, please enter your personal information below.</h5>
            </div>
            <div style="font-size: 13.5pt;">
              <span class="label">Username:</span>
              <input name="username" class="text" type="text">
              <span class="label">Password:</span>
              <input name="password" class="text" type="password">
              <span class="label">First Name:</span>
              <input name="firstname" class="text" type="text">
              <span class="label">Last Name:</span>
              <input class="text" type="text" name="lastname">
              <span class="label">Height:</span>
              <input name="height short" id="height" class="text" type="tel" pattern="[0-9]*" novalidate>
              <span class="label">Weight:</span>
              <input name="weight" class="text short" type="tel" pattern="[0-9]*" style="display:inline-block;"> LBS
              <br>
              <span class="label">Age:</span>
              <input name="age" id="age" class="text short" type="tel" pattern="[0-9]*" style="width: 50%;height: 35px;">
              <span class="label">Email:</span>
              <input class="text" type="text" name="email">
              <span class="label">Phone Number:</span>
              <input name="phone" class="text" type="text">
            </div>
            <br>
            <input type="submit" value="Next" style="width: 120px;">
        </form>
      </div>
  </div>

</body>

</html>